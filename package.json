{
  "name": "baseledger-staking-test",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ schmidsi/baseledger-staking-test",
    "create-local": "graph create --node http://localhost:8020/ schmidsi/baseledger-staking-test",
    "remove-local": "graph remove --node http://localhost:8020/ schmidsi/baseledger-staking-test",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 schmidsi/baseledger-staking-test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.21.1",
    "@graphprotocol/graph-ts": "0.20.0"
  }
}
